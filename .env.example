# The last two digits of the number. Leave empty to accept any ending.
VITE_ENDING=
# Optional: a phone number the "Sig det til ham" button texts. Leave empty to hide it.
VITE_REPLY_TO=
# The deployed origin, without a trailing slash. Used for the absolute share image URL.
VITE_SITE_URL=https://example.com
